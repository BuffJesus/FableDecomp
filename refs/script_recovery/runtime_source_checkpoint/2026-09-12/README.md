# Runtime source checkpoint

Tracked changes are in runtime.patch against the base commit in provenance.json.
The added/ directory contains the three new headers not represented by git diff.
This preserves the runtime checkout, which has no configured Git remote.
No local checkout files were modified. This is source evidence, not a tested
release package; consult the September 12 overnight handoff for installed and
live-tested build distinctions.
