-- Isolated ForgeFSE registry for the retail Q_NewOakValeIntro allocator playtest.
-- Retail replacement is configured separately by FSE/retail_override.lua, so no
-- custom quest entry belongs here. This avoids duplicate identities and unrelated
-- always-on test quests while preserving FinalAlbion.qst's native quest registry.
Quests = {}
