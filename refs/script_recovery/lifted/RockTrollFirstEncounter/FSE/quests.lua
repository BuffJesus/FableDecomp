-- Lifted draft; NOT registered by default (competes with the retail allocator if enabled).
Quests = {}
-- Quests.RockTrollFirstEncounter = { file = "RockTrollFirstEncounter/RockTrollFirstEncounter", enabled = false }
