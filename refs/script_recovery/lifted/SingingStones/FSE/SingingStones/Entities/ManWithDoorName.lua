-- TODO(native): entity script "ManWithDoorName" needs its translation-unit export; lift it with
-- lift_native_lua.py --tu <translation_unit.json> --entity ManWithDoorName --init 0x... --main 0x...
local MY_SCRIPT_NAME = "ManWithDoorName"

function Init(quest, me)
end

function Main(quest, me)
    -- TODO(native): body not lifted
end
