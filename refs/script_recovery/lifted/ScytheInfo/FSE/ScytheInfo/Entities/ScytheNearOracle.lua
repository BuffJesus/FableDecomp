-- TODO(native): entity script "ScytheNearOracle" needs its translation-unit export; lift it with
-- lift_native_lua.py --tu <translation_unit.json> --entity ScytheNearOracle --init 0x... --main 0x...
local MY_SCRIPT_NAME = "ScytheNearOracle"

function Init(quest, me)
end

function Main(quest, me)
    -- TODO(native): body not lifted
end
