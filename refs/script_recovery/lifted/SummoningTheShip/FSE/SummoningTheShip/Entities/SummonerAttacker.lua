-- TODO(native): entity script "SummonerAttacker" needs its translation-unit export; lift it with
-- lift_native_lua.py --tu <translation_unit.json> --entity SummonerAttacker --init 0x... --main 0x...
local MY_SCRIPT_NAME = "SummonerAttacker"

function Init(quest, me)
end

function Main(quest, me)
    -- TODO(native): body not lifted
end
