-- TODO(native): entity script "M_FishermanWaspSpawn" needs its translation-unit export; lift it with
-- lift_native_lua.py --tu <translation_unit.json> --entity M_FishermanWaspSpawn --init 0x... --main 0x...
local MY_SCRIPT_NAME = "M_FishermanWaspSpawn"

function Init(quest, me)
end

function Main(quest, me)
    -- TODO(native): body not lifted
end
