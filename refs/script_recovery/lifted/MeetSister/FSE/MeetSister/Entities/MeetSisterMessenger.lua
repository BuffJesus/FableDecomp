-- TODO(native): entity script "MeetSisterMessenger" needs its translation-unit export; lift it with
-- lift_native_lua.py --tu <translation_unit.json> --entity MeetSisterMessenger --init 0x... --main 0x...
local MY_SCRIPT_NAME = "MeetSisterMessenger"

function Init(quest, me)
end

function Main(quest, me)
    -- TODO(native): body not lifted
end
