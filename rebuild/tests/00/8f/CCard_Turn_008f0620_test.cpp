#include <stdio.h>
struct CCard { void* Thing; long Name; long Value; float InitialRotation; float FinalRotation; float TurnSpeed; float TurnFraction; float MoveSpeed; float MoveFraction; bool Turning; bool FaceDown; bool Moving; unsigned char Tail[49]; void Turn(); }; int main() { CCard card = {}; card.FaceDown = true; card.TurnFraction = 0.75f; card.Turn(); if (!card.Turning || card.FaceDown || card.TurnFraction != 0.0f || card.InitialRotation != 0.5f || card.FinalRotation != 0.0f) return 1; card.Turn(); if (!card.FaceDown || card.InitialRotation != 0.0f || card.FinalRotation != 0.5f) return 2; printf("CARD_TURN PASS\n"); return 0; }