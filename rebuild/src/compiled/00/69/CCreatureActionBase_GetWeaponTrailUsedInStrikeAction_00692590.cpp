#include "engine/CCreatureActionBase.h"  // retyped onto the PDB layout; byte parity re-verified
struct CCreatureActionBase_Methods : CCreatureActionBase {
    int M(int a0);
};
int CCreatureActionBase_Methods::M(int a0) { return 0; }